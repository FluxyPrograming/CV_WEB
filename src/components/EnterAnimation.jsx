import { gsap } from "gsap";
import { useGSAP } from "@gsap/react";

gsap.registerPlugin(useGSAP);

export const EnterAnimation = () => {
  return (
    <>
      <div id="" className="m-auto bg-primary h-40 w-3/5 rounded-xl"></div>
    </>
  );
};
